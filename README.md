"# Locopilot-Alert-System-using-MERN-Python" 
"# Locopilot-Alert-System-using-MERN-Python" 

after cloning give the command to install libraries:

npm install or npm i

To Run the BACKEND(need mongodb compass for local db):

use the command at terminal(after cloning the project)

cd backend
npm run dev

To Run the FRONTEND:

use the command at terminal(after cloning the project)

cd frontend\mlpro
npm run dev

